apiVersion: v1
kind: Service
metadata:
  name: prod-book-import
  labels:
    name: prod-book-import
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    name: prod-book-import

---

apiVersion: v1
kind: Service
metadata:
  name: test-book-import
  labels:
    name: test-book-import
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    name: test-book-import